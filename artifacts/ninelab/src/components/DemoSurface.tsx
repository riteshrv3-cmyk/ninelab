// Naming re-export: the demo-surface system lives in DemoBanner.tsx (single
// file avoids an import cycle between the banner, the chip, and the context).
export { DemoSurface, DemoBanner, SampleChip } from "./DemoBanner";
